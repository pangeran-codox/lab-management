import './bootstrap';

// Alpine.js sudah di-bundle dan distart otomatis oleh Livewire v3
// via @livewireScripts di app-layout.blade.php.
// Jangan import ulang di sini — akan menyebabkan "multiple instances" warning.
//
// Jika ada komponen Alpine non-Livewire, daftarkan via:
//   document.addEventListener('alpine:init', () => { ... })
